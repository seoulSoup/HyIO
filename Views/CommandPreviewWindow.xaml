<Window x:Class="HyIO.Views.CommandPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="340"
        Height="Auto"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        KeyDown="Window_KeyDown"
        Deactivated="Window_Deactivated">
    <Border CornerRadius="22"
            Background="#F8FFFFFF"
            BorderBrush="#22000000"
            BorderThickness="1"
            Padding="14">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0"
                              BlurRadius="20"
                              Opacity="0.28"/>
        </Border.Effect>

        <Grid Width="332">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="CommandTextBlock"
                       Grid.ColumnSpan="2"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource AppTextPrimaryBrush}"
                       FontFamily="{StaticResource AppFontFamily}"/>

            <TextBlock Grid.Row="1"
                       Grid.ColumnSpan="2"
                       Margin="0,6,0,10"
                       Text="마우스로 클릭하거나 ↑/↓ 후 Enter로 선택"
                       Foreground="{StaticResource AppTextSecondaryBrush}"
                       FontSize="11"
                       FontFamily="{StaticResource AppFontFamily}"/>

            <ListBox x:Name="CandidatesList"
                     Grid.Row="2"
                     Grid.Column="0"
                     MaxHeight="520"
                     BorderThickness="0"
                     Background="Transparent"
                     FocusVisualStyle="{x:Null}"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="CandidatesList_MouseDoubleClick"
                     MouseLeftButtonUp="CandidatesList_MouseLeftButtonUp">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,8"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Root"
                                            CornerRadius="16"
                                            Background="#FFF8FAFD"
                                            BorderBrush="Transparent"
                                            BorderThickness="1"
                                            Padding="10">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="#FFEFF4FF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="#FFE7F0FF"/>
                                            <Setter TargetName="Root" Property="BorderBrush" Value="#FF86A7FF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="72"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="64"
                                    Height="64"
                                    CornerRadius="12"
                                    Background="#FFF5F6FA"
                                    ClipToBounds="True">
                                <Image Source="{Binding Thumbnail}"
                                       Stretch="UniformToFill"
                                       SnapsToDevicePixels="True"/>
                            </Border>

                            <StackPanel Grid.Column="1"
                                        Margin="12,0,0,0"
                                        VerticalAlignment="Center">
                                <TextBlock Text="{Binding FileName}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource AppTextPrimaryBrush}"
                                           FontFamily="{StaticResource AppFontFamily}"/>
                                <TextBlock Text="{Binding MatchSummary}"
                                           Margin="0,4,0,0"
                                           FontSize="11"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource AppTextSecondaryBrush}"
                                           FontFamily="{StaticResource AppFontFamily}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border x:Name="CustomScrollTrack"
                    Grid.Row="2"
                    Grid.Column="1"
                    Width="20"
                    Margin="0"
                    Background="{StaticResource TagScrollBarTrackBackgroundBrush}"
                    CornerRadius="10"
                    SnapsToDevicePixels="True"
                    SizeChanged="CustomScrollTrack_SizeChanged">
                <Grid>
                    <Border x:Name="CustomScrollThumb"
                            Background="{StaticResource TagScrollBarThumbBackgroundBrush}"
                            CornerRadius="8"
                            Margin="4"
                            VerticalAlignment="Top"
                            Height="40"
                            Cursor="Hand"
                            MouseLeftButtonDown="CustomScrollThumb_MouseLeftButtonDown"
                            MouseLeftButtonUp="CustomScrollThumb_MouseLeftButtonUp"
                            MouseMove="CustomScrollThumb_MouseMove" />
                </Grid>
            </Border>

            <Border x:Name="EmptyStatePanel"
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    CornerRadius="16"
                    Background="#FFF8FAFD"
                    Padding="16"
                    Visibility="Collapsed">
                <TextBlock Text="명령어를 찾을 수 없어요"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"
                           Foreground="{StaticResource AppTextSecondaryBrush}"
                           FontSize="12"
                           FontFamily="{StaticResource AppFontFamily}"/>
            </Border>
        </Grid>
    </Border>
</Window>
