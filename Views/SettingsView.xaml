<UserControl x:Class="HyIO.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,12">
            <TextBlock Text="글로벌 핫키"
                       Foreground="{StaticResource AppTextPrimaryBrush}"
                       FontFamily="{StaticResource AppFontFamily}"
                       FontSize="14"/>
            <TextBox x:Name="HotkeyBox"
                     Width="200"
                     Height="30"
                     Margin="0,4,0,0"
                     Padding="8,4"
                     FontFamily="{StaticResource AppFontFamily}"
                     Foreground="{StaticResource AppTextPrimaryBrush}"
                     Background="{StaticResource AppSurfaceAltBrush}"
                     BorderBrush="{StaticResource AppBorderBrush}"/>
            <TextBlock Text="예: Ctrl+Space, Ctrl+Alt+O"
                       Foreground="{StaticResource AppTextSecondaryBrush}"
                       FontFamily="{StaticResource AppFontFamily}"
                       FontSize="11"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <CheckBox x:Name="AutoPasteCheck"
                      Content="이미지 선택 시 자동으로 Ctrl+V 보내기"
                      IsThreeState="False"
                      VerticalAlignment="Center"
                      FontFamily="{StaticResource AppFontFamily}"
                      Foreground="{StaticResource AppTextPrimaryBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="설정 저장"
                    Click="SaveButton_Click"
                    Padding="16,4"/>
        </StackPanel>
    </Grid>
</UserControl>
