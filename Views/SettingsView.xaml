<UserControl x:Class="HyIO.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework">
    <Grid>
        <Grid.Resources>
            <Style x:Key="SettingsActionButtonStyle" TargetType="Button">
                <Setter Property="MinWidth" Value="128"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Padding" Value="18,6"/>
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{StaticResource ButtonOrangeMiddleBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="14">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Root"
                                            Property="Background"
                                            Value="{StaticResource ButtonOrangeHighBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Root"
                                            Property="Background"
                                            Value="#FFD96A07"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="AutoPasteToggleStyle" TargetType="primitives:ToggleButton">
                <Setter Property="Width" Value="68"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Background" Value="#FFD7DCE8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="primitives:ToggleButton">
                            <Grid>
                                <Border x:Name="Track"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="17"/>
                                <Border x:Name="Thumb"
                                        Width="26"
                                        Height="26"
                                        Margin="4,0,0,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Background="White"
                                        CornerRadius="13">
                                    <Border.RenderTransform>
                                        <TranslateTransform/>
                                    </Border.RenderTransform>
                                </Border>
                                <TextBlock x:Name="StateText"
                                           Margin="0,0,10,0"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           FontFamily="{StaticResource AppFontFamily}"
                                           FontSize="12"
                                           Foreground="{StaticResource AppTextSecondaryBrush}"
                                           Text="OFF"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{StaticResource ButtonOrangeMiddleBrush}"/>
                                    <Setter TargetName="StateText" Property="Text" Value="ON"/>
                                    <Setter TargetName="StateText" Property="HorizontalAlignment" Value="Left"/>
                                    <Setter TargetName="StateText" Property="Margin" Value="10,0,0,0"/>
                                    <Setter TargetName="StateText" Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Track" Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Vertical"
                    Margin="0,0,0,16"
                    HorizontalAlignment="Left">
            <TextBlock Text="오버레이 실행 단축키"
                       Foreground="{StaticResource AppTextPrimaryBrush}"
                       FontFamily="{StaticResource AppFontFamily}"
                       FontSize="16"/>
            <TextBox x:Name="HotkeyBox"
                     Width="200"
                     Height="34"
                     Margin="0,4,0,0"
                     Padding="8,4"
                     HorizontalAlignment="Left"
                     IsReadOnly="True"
                     FontFamily="{StaticResource AppFontFamily}"
                     FontSize="14"
                     Foreground="{StaticResource AppTextPrimaryBrush}"
                     Background="{StaticResource AppSurfaceAltBrush}"
                     BorderBrush="{StaticResource AppBorderBrush}"
                     FocusVisualStyle="{x:Null}"
                     PreviewKeyDown="HotkeyBox_PreviewKeyDown"/>
            <TextBlock Text="Example: Ctrl+Space, Ctrl+Alt+O"
                       Foreground="{StaticResource AppTextSecondaryBrush}"
                       FontFamily="{StaticResource AppFontFamily}"
                       FontSize="13"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Height="1"
                Margin="0,0,0,100"
                Background="{StaticResource AppBorderBrush}"/>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <primitives:ToggleButton x:Name="AutoPasteToggle"
                                      Grid.Column="0"
                                      Style="{StaticResource AutoPasteToggleStyle}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,14,0"
                                      Click="AutoPasteToggle_Click"
                                      Loaded="AutoPasteToggle_Loaded"
                                      Checked="AutoPasteToggle_Checked"
                                      Unchecked="AutoPasteToggle_Unchecked"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="자동 붙여넣기"
                           Foreground="{StaticResource AppTextPrimaryBrush}"
                           FontFamily="{StaticResource AppFontFamily}"
                           FontSize="16"/>
                <TextBlock x:Name="AutoPasteDescription"
                           Margin="0,4,0,0"
                           Foreground="{StaticResource AppTextSecondaryBrush}"
                           FontFamily="{StaticResource AppFontFamily}"
                           FontSize="13"
                           Text="이미지 선택 후 클립보드 복사만 수행합니다."/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
